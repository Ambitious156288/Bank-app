import React from 'react';
import { storiesOf } from '@storybook/react';
import SnackbarAlert from './SnackbarAlert';

storiesOf('Atoms/SnackbarAlert', module).add('Normal', () => <SnackbarAlert />);
